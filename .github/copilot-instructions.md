# UTSAVX workspace

- Frontend: React + Vite + Redux Toolkit + Tailwind CSS.
- Backend: Node.js + Express + MongoDB + Redis.
- Keep API modules under `server/src` and UI modules under `client/src`.
- Use environment variables for all credentials and external integrations.
